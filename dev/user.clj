(ns user
  "Loaded automatically by `clj -M:dev`. The modules themselves are ClojureScript —
   use `npx shadow-cljs` (or the :test build) to actually compile them; this namespace
   is here for JVM-side tooling and scratch work.")
